import Anthropic from "@anthropic-ai/sdk";export const anthropic = new Anthropic({apiKey: process.env.ANTHROPIC_API_KEY});export const HAIKU_MODEL = "claude-haiku-4-5-20251001";export const SONNET_MODEL = "claude-sonnet-4-6";export const COACH_SYSTEM_PROMPT = "You are Vitality, a concise fitness coach. Be specific, cite numbers, keep replies short. Not a doctor.";export async function generateDailyAdvice(prompt){const r=await anthropic.messages.create({model:HAIKU_MODEL,max_tokens:400,system:COACH_SYSTEM_PROMPT,messages:[{role:"user",content:prompt}]});return r.content.find(b=>b.type==="text")?.text??"";}export async function chatWithCoach(history){const r=await anthropic.messages.create({model:HAIKU_MODEL,max_tokens:500,system:COACH_SYSTEM_PROMPT,messages:history});return r.content.find(b=>b.type==="text")?.text??"";}
